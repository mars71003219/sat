name: "vae_timeseries"
backend: "python"
max_batch_size: 64

# 입력 정의
input [
  {
    name: "INPUT"
    data_type: TYPE_FP32
    dims: [ 1, 20 ]
  }
]

# 출력 정의
output [
  {
    name: "OUTPUT"
    data_type: TYPE_FP32
    dims: [ 10 ]
  }
]

# Dynamic Batching 설정
dynamic_batching {
  preferred_batch_size: [ 4, 8, 16, 32 ]
  max_queue_delay_microseconds: 100000
}

# 인스턴스 설정 - GPU 사용
instance_group [
  {
    count: 2
    kind: KIND_GPU
    gpus: [ 0 ]
  }
]

# 파라미터 (선택사항)
parameters: {
  key: "sequence_length"
  value: {
    string_value: "20"
  }
}
parameters: {
  key: "forecast_steps"
  value: {
    string_value: "10"
  }
}
parameters: {
  key: "latent_dim"
  value: {
    string_value: "32"
  }
}
parameters: {
  key: "hidden_dim"
  value: {
    string_value: "64"
  }
}
